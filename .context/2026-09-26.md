## 09:49 — Contract-Work Openness Wired In; Two New Postings Evaluated; Sequoia Board Added

Decisions:
- Charles: "actually i'm open for contract work as well." Asked scope (record-only vs. widen filters vs. full build-out) and type (fractional/interim/W2-contract/1099 — chose all). Answer: full build-out, all arrangement types.
- Added `modes/_profile.md` → "Your Openness to Contract Work": fractional/interim, W2 contract-to-hire, and 1099 consulting now in scope at the same T1-T4 archetypes, with a day-rate/hourly comp-translation table off the existing $140K floor. `modes/_custom.md` house rule: never screen out or score down non-permanent postings.
- `portals.yml`: 37 new title-filter positive keywords (fractional/interim/contract/consultant × role terms) + `Fractional`/`Interim` seniority boosts + 2 new ATS-wide search_queries for fractional/interim/contract marketing leadership on Greenhouse/Lever/Ashby.
- Fanned out 5 parallel research agents to live-test 13 fractional/contract/gig marketplaces and staffing firms (Braintrust, Contra, Bolster, Wripple, Malt, Toptal, Catalant, Business Talent Group, MBO Partners, Chief Outsiders, Chameleon Collective, RevPartners, The CMO Club) against the zero-auth/no-browser provider contract. **None qualify** — each is closed, gated behind joining a private network, or on an unsupported ATS. Braintrust came closest (genuine public JSON API, no AI-bot robots.txt block) but independently re-verified its `robots.txt` disallows `page=` query params (blocks the pagination a 134-listing board needs) and it's a general tech-freelance marketplace where working a listing still requires a free Braintrust profile — same "join the network" pattern as the rejected ones. Recorded all 13 in `docs/SUPPORTED_JOB_BOARDS.md` (pushed, commits `838f7d2`/`4a769a9`).
- Re-ran the zero-token scan under the widened filters: immediately surfaced 2 new postings that would have been filtered out before today — Minerva University (Marketing Analytics & Insights Consultant, Independent Consultant contract) and Nutrafol (Interim Director, Marketplaces, 6mo contract). Confirms the widened filter has real pull, not just theoretical coverage.
- Ran both through the full pipeline (parallel subagent evaluations, reserved report #067/#068): Minerva scored 4.0/5 (Apply — strong T1/T3 fit, JD itself calls for "a fractional marketing analytics leader"; no rate stated, flag to confirm before applying). Nutrafol scored 2.3/5 (Skip — core requirement is hands-on Amazon Seller Central/Walmart Seller Center ops, entirely absent from the CV, no ramp time on an interim seat; also caught and flagged a likely Built In unit-mislabel on the stated "$80-100 Annually" rate — almost certainly hourly for a 6-month contract).
- Followed up on the still-open VC-board question (a16z/Sequoia/ICONIQ/Redpoint) without re-asking, per continue-working directive: **Sequoia Capital** verified working on the existing `consider` provider (`consider_board: sequoia-capital`, `jobs.sequoiacap.com`) — added to `portals.yml`, confirmed 500 live postings via `verify-portals.mjs`. a16z runs a real but *newer* Consider frontend whose `/api-boards/search-jobs` endpoint 404s under every board-id guess (needs its own provider variant, not built today). ICONIQ's Greenhouse board is the firm's own internal hiring, not a portfolio network. Redpoint has no discoverable public portfolio board at all. All three non-hits recorded in `docs/SUPPORTED_JOB_BOARDS.md` alongside Sequoia's addition.
- Final scan with the 10th board (Sequoia) live: clean run, 74 companies/10 boards/13,229 jobs seen, 0 errors, 0 new offers this pass (expected — Sequoia's 500 postings just didn't hit today's filter).

Next: Resume normal `/career-ops scan` → `pipeline` cadence against the widened 90-company/10-board roster with the new contract/fractional filters live. If a16z's portfolio board is worth chasing later, it needs a headless network capture to find the real (non-`/api-boards/search-jobs`) data endpoint before a new provider variant can be written.
Skills: career-ops (scan, pipeline)

## 11:31 — Applied-Tier Pipeline: 2 PDFs Built, HM-Audited, 1 Discarded

Decisions:
- Executed "apply to all 3" against the 4.0/5-clearing evaluated queue: Netskope (#63), Minerva University (#67), Kyndryl (#24).
- Kyndryl (#24): status set to Discarded (`set-status.mjs --report 24 Discarded`) — Workday's own telemetry-domain block crashes the "How Did You Hear About Us?" multiselect in every automated environment tried; Charles declined further manual chase.
- Netskope (#63): tailored ATS PDF generated and fact-gated (`output/cv-charles-blain-netskope-2026-09-26.pdf`), tracker synced. Traced the real application off Built In to Netskope's own Greenhouse posting (req R-004949, gh_jid=8207334, https://www.netskope.com/company/careers/open-positions/?gh_jid=8207334) via the Greenhouse public API; pulled the exact field list and drafted every answer into `## Application Answers` on reports/063-netskope-2026-09-25.md. Could not click through the live embedded form — this sandboxed browser can't render Greenhouse's iframe (repeatable navigation timeout, same failure class as Kyndryl's Workday block).
- Minerva (#67): tailored PDF generated and fact-gated (`output/cv-charles-blain-minerva-university-2026-09-26.pdf`), tracker synced. Confirmed the live canonical posting on Minerva's own Comeet board (https://www.comeet.com/jobs/minerva/3A.007/marketing-analytics--insights-consultant/F0.37C); Comeet renders client-side with no accessible API, so exact fields weren't enumerable — drafted standard identity fields and flagged the unstated-rate + 30-35hr/wk capacity questions for Charles to raise directly with Minerva (persisted to reports/067-minerva-university-2026-09-26.md). Charles confirmed he's open to leaving the current SVP role, so capacity isn't a blocker.
- Ran the `modes/pdf/hm-audit.md` adversarial hiring-manager pass (two parallel subagents) on both tailored CVs after both passed the deterministic `verify-ats.mjs` check (100/100 each). **Netskope verdict: would NOT advance to screen** — zero evidence anywhere of the JD's three named required systems (Salesforce/Marketo/Clay), a real capability gap no rewrite fixes; secondary fixes recommended (cut bullets 16/19, rewrite 2/3). **Minerva verdict: would advance, narrowly** — real risk is bullets 7-9 (BigQuery/Python/data-governance framing) reading as exactly the profile the JD twice explicitly excludes ("NOT primarily data engineering, BI, or dashboard development"); recommended cutting bullet 9 outright and rewriting 7/8/11/17/18. Full tables: `agent://NetskopeAudit`, `agent://MinervaAudit` (short-lived job IDs — re-derive from `history://NetskopeAudit`/`history://MinervaAudit` if expired). Neither audit's rewrites applied to the CVs yet.

Next: Get Charles's call on applying the HM-audit rewrites (Netskope cut #16/#19 + rewrite #2/#3; Minerva cut #9 + rewrite #7/#8/#11/#17/#18) and regenerating both PDFs; then Charles completes the live-form submission himself at the Netskope Greenhouse link and Minerva Comeet link (this environment's browser can't reach either).
Skills: career-ops (pdf, apply)

## (later) — HM-Audit Rewrites Applied, Jev-Cross-Checked, Both PDFs Regenerated

Decisions:
- Charles's call on the pending HM-audit rewrites: apply both sets (Netskope cut #16/#19 + rewrite #2/#3; Minerva cut #9 + rewrite #7/#8/#11/#17/#18).
- Applied all six cuts/rewrites directly to `output/cv-charles-blain-netskope.html` and `output/cv-charles-blain-minerva-university.html` (bullet numbering = the `<li>` items in Work Experience only, 19 per CV, confirmed against `modes/pdf/hm-audit.md`'s numbering convention since the original audit's per-bullet table had already expired as process-local job output and was not recoverable).
- Introduced a second verification layer beyond the mechanical `verify-cv-facts.mjs`/`verify-ats.mjs` gates: batch-judged every cut and rewrite through `typesafe/jev-1.13.0` (the harness's bounded-judgment model, reached via the `judge` role's fallback chain even though the raw Jev API key/device is not configured in this environment). All three cuts (Netskope 16/19, Minerva 9) independently confirmed correct (P(keep it) 0.02-0.16; Minerva 9 scored 3.82/4 "severe" screen-risk if left in). Two first-draft rewrites (Minerva bullets 11 and 17) flagged P(invents a claim beyond source) = 0.56 and 0.75 for clauses not literally present in that bullet's own `cv.md` source line (an added diagnostic clause on 11, an added "full-lifecycle... self-directed start to finish" clause on 17); both were pulled back to the literal source before shipping. Netskope bullet 3 scored a coin-flip (0.50) on an added "prioritize accounts" clause; also trimmed back to a pure subtraction of the original overclaim.
- Regenerated both PDFs (`output/cv-charles-blain-netskope-2026-09-26.pdf`, `output/cv-charles-blain-minerva-university-2026-09-26.pdf`) via `generate-pdf.mjs`. Both re-passed the fact gate and scored ATS 100/100 (A) after the rewrite. Both render at 3 pages against the script's 2-page default (warning-only, pre-existing — unrelated to today's edit, since every change either removed a bullet or swapped one of equal length). Flagged to Charles; his call: keep 3 pages, no further trimming.
- Persisted a `## HM Audit` section (with a `Jev cross-check` sub-note) to both `reports/063-netskope-2026-09-25.md` and `reports/067-minerva-university-2026-09-26.md`, per `modes/pdf/hm-audit.md` Step 5 — honestly marked as a reconstruction (the original per-bullet table could not be recovered) rather than fabricated verbatim.

Next: Charles completes the live-form submission himself at the Netskope Greenhouse link (req R-004949) and the Minerva Comeet link — this sandboxed browser still cannot reach either. No further action pending on these two applications.
Skills: career-ops (pdf, apply, hm-audit)
